import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
  loaderContainer: {
    width: '100%',
    height: '100%',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  },
  loaderStyles: {
    color: 'orange',
  },
});
